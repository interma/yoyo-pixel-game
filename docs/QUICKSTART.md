# 🚀 快速开始指南

使用 Makefile 快速开始开发和测试。

## 📦 首次设置

```bash
# 安装所有依赖（包括测试工具）
make setup
```

这将安装：
- 项目依赖
- Vitest（单元测试）
- Playwright（E2E测试）

## 🎮 开发工作流

### 启动开发
```bash
make dev
```
访问 http://localhost:5173

### 构建项目
```bash
make build
```
输出到 `dist/` 目录

### 预览构建
```bash
make preview
```

## 🧪 运行测试

### 运行所有测试
```bash
make test
```

### 单独运行单元测试
```bash
make test-unit
```

### 单独运行E2E测试
```bash
make test-e2e
```

### 可视化测试界面
```bash
make test-ui
```

### 生成覆盖率报告
```bash
make test-coverage
```

### 监控模式（自动重测）
```bash
make watch
```

## 🔍 代码质量

### 代码检查
```bash
make lint
```

### 格式化代码
```bash
make format
```

### 提交前检查（推荐）
```bash
make check
```
这会运行：lint + test + build

## 🧹 清理

### 清理构建文件
```bash
make clean
```

### 深度清理（包括 node_modules）
```bash
make clean-all
```

## 📊 查看统计

```bash
make stats
```
显示：
- 文件数量
- 代码行数
- 依赖统计
- 构建大小

## 🚀 部署

```bash
make deploy
```
显示部署提示（Vercel/GitHub Pages）

## 💡 常用命令组合

### 开发流程
```bash
make install      # 安装依赖
make dev         # 启动开发
```

### 测试流程
```bash
make test-unit    # 快速测试逻辑
make test-e2e     # 测试用户流程
```

### 发布流程
```bash
make check       # 完整检查
make build       # 构建
make deploy      # 部署
```

## 📚 获取帮助

```bash
make help
```
或者直接运行 `make`（默认显示帮助）

## 🎯 快捷键提示

所有命令都以 `make` 开头，例如：
- `make dev` = `npm run dev`
- `make build` = `npm run build`
- `make test` = 运行所有测试

更短、更好记！✨
